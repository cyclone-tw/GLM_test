# 3D 動畫抽籤系統實作計畫

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 建立一個可自訂名單和獎項的 3D 動畫抽籤 HTML 應用程式

**Architecture:** 單一 HTML 檔案包含所有 CSS 和 JavaScript，使用 Three.js 實現 3D 球體動畫，localStorage 儲存資料，支援多主題切換

**Tech Stack:** HTML5, CSS3, JavaScript (ES6+), Three.js, html2canvas, Google Sheets API

---

## 檔案結構

```
lottery.html    # 單一 HTML 檔案（內含 CSS、JavaScript、音效 base64）
```

---

## Task 1: 基礎 HTML 結構與 CSS 樣式

**Files:**
- Create: `lottery.html`

- [ ] **Step 1: 建立 HTML 基礎結構**

```html
<!DOCTYPE html>
<html lang="zh-TW">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>3D 抽籤系統</title>
    <script src="https://cdn.jsdelivr.net/npm/three@0.160.0/build/three.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/html2canvas@1.4.1/dist/html2canvas.min.js"></script>
    <style>
        /* CSS 將在後續步驟加入 */
    </style>
</head>
<body>
    <!-- UI 結構將在後續步驟加入 -->
    <script>
        // JavaScript 將在後續步驟加入
    </script>
</body>
</html>
```

- [ ] **Step 2: 加入 CSS Reset 與基礎變數**

在 `<style>` 標籤內加入：

```css
/* CSS Reset */
*, *::before, *::after {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
}

:root {
    /* 預設主題變數 - 科技感 */
    --bg-color: #0a0a1a;
    --text-color: #ffffff;
    --accent-color: #00ffff;
    --particle-color: #00ffff;
    --card-bg: rgba(255, 255, 255, 0.1);
    --border-color: rgba(255, 255, 255, 0.2);
}

body {
    font-family: 'Microsoft JhengHei', 'PingFang TC', sans-serif;
    background-color: var(--bg-color);
    color: var(--text-color);
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    overflow-x: hidden;
}
```

- [ ] **Step 3: 加入 UI 佈局 CSS**

```css
/* 頂部工具列 */
.toolbar {
    display: flex;
    justify-content: flex-end;
    gap: 10px;
    padding: 10px 20px;
    background: rgba(0, 0, 0, 0.3);
    border-bottom: 1px solid var(--border-color);
}

.toolbar button, .toolbar select {
    padding: 8px 16px;
    border: 1px solid var(--border-color);
    background: var(--card-bg);
    color: var(--text-color);
    border-radius: 4px;
    cursor: pointer;
    font-size: 14px;
}

.toolbar button:hover, .toolbar select:hover {
    background: rgba(255, 255, 255, 0.2);
}

/* 3D 動畫區域 */
.canvas-container {
    flex: 1;
    min-height: 400px;
    position: relative;
}

#canvas3d {
    width: 100%;
    height: 100%;
    display: block;
}

/* 2D 備用模式 */
.fallback-2d {
    display: none;
    width: 100%;
    height: 100%;
    overflow: hidden;
    position: relative;
}

.fallback-2d .name-item {
    display: inline-block;
    padding: 10px 20px;
    margin: 5px;
    background: var(--card-bg);
    border-radius: 8px;
    font-size: 18px;
    white-space: nowrap;
}

/* 控制區域 */
.control-panel {
    padding: 20px;
    text-align: center;
    background: rgba(0, 0, 0, 0.3);
    border-top: 1px solid var(--border-color);
}

.prize-display {
    font-size: 24px;
    margin-bottom: 15px;
}

.prize-display .prize-name {
    color: var(--accent-color);
    font-weight: bold;
}

.draw-button {
    padding: 15px 50px;
    font-size: 20px;
    background: linear-gradient(135deg, var(--accent-color), #0088ff);
    border: none;
    border-radius: 30px;
    color: #000;
    font-weight: bold;
    cursor: pointer;
    transition: transform 0.2s, box-shadow 0.2s;
}

.draw-button:hover {
    transform: scale(1.05);
    box-shadow: 0 0 20px var(--accent-color);
}

.draw-button:disabled {
    opacity: 0.5;
    cursor: not-allowed;
    transform: none;
}

/* 分頁區域 */
.tabs-container {
    border-top: 1px solid var(--border-color);
}

.tab-buttons {
    display: flex;
    background: rgba(0, 0, 0, 0.3);
}

.tab-btn {
    flex: 1;
    padding: 12px;
    border: none;
    background: transparent;
    color: var(--text-color);
    cursor: pointer;
    border-bottom: 3px solid transparent;
    font-size: 14px;
}

.tab-btn.active {
    border-bottom-color: var(--accent-color);
    background: rgba(255, 255, 255, 0.1);
}

.tab-content {
    display: none;
    padding: 15px;
    max-height: 300px;
    overflow-y: auto;
}

.tab-content.active {
    display: block;
}

/* 列表樣式 */
.list-actions {
    display: flex;
    gap: 8px;
    margin-bottom: 15px;
    flex-wrap: wrap;
}

.list-actions button {
    padding: 6px 12px;
    border: 1px solid var(--border-color);
    background: var(--card-bg);
    color: var(--text-color);
    border-radius: 4px;
    cursor: pointer;
    font-size: 13px;
}

.item-list {
    list-style: none;
}

.item-list li {
    display: flex;
    align-items: center;
    padding: 8px 12px;
    background: var(--card-bg);
    margin-bottom: 5px;
    border-radius: 4px;
    cursor: grab;
}

.item-list li:active {
    cursor: grabbing;
}

.item-list .item-name {
    flex: 1;
}

.item-list .item-actions button {
    padding: 2px 8px;
    margin-left: 5px;
    border: none;
    background: rgba(255, 255, 255, 0.1);
    color: var(--text-color);
    border-radius: 3px;
    cursor: pointer;
    font-size: 12px;
}

/* 彈窗樣式 */
.modal {
    display: none;
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0, 0, 0, 0.8);
    z-index: 1000;
    justify-content: center;
    align-items: center;
}

.modal.show {
    display: flex;
}

.modal-content {
    background: var(--bg-color);
    border: 1px solid var(--border-color);
    border-radius: 8px;
    padding: 20px;
    max-width: 500px;
    width: 90%;
    max-height: 80vh;
    overflow-y: auto;
}

.modal-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 15px;
}

.modal-header h3 {
    color: var(--accent-color);
}

.modal-close {
    background: none;
    border: none;
    color: var(--text-color);
    font-size: 24px;
    cursor: pointer;
}

.modal textarea {
    width: 100%;
    height: 150px;
    background: var(--card-bg);
    border: 1px solid var(--border-color);
    color: var(--text-color);
    border-radius: 4px;
    padding: 10px;
    font-size: 14px;
    resize: vertical;
}

.modal input[type="text"] {
    width: 100%;
    padding: 10px;
    background: var(--card-bg);
    border: 1px solid var(--border-color);
    color: var(--text-color);
    border-radius: 4px;
    margin-bottom: 10px;
}

.modal button.primary {
    width: 100%;
    padding: 10px;
    background: var(--accent-color);
    border: none;
    border-radius: 4px;
    color: #000;
    font-weight: bold;
    cursor: pointer;
    margin-top: 10px;
}

/* 中獎者顯示 Overlay */
.winner-overlay {
    display: none;
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0, 0, 0, 0.9);
    z-index: 2000;
    justify-content: center;
    align-items: center;
    flex-direction: column;
}

.winner-overlay.show {
    display: flex;
}

.winner-name {
    font-size: 60px;
    font-weight: bold;
    color: var(--accent-color);
    text-shadow: 0 0 30px var(--accent-color);
    animation: glow 1s ease-in-out infinite alternate;
}

.winner-prize {
    font-size: 30px;
    margin-top: 20px;
    color: var(--text-color);
}

@keyframes glow {
    from {
        text-shadow: 0 0 30px var(--accent-color);
    }
    to {
        text-shadow: 0 0 60px var(--accent-color), 0 0 80px var(--accent-color);
    }
}

.winner-actions {
    margin-top: 30px;
    display: flex;
    gap: 15px;
}

.winner-actions button {
    padding: 10px 25px;
    border: 2px solid var(--accent-color);
    background: transparent;
    color: var(--text-color);
    border-radius: 25px;
    cursor: pointer;
    font-size: 16px;
}

.winner-actions button:hover {
    background: var(--accent-color);
    color: #000;
}

/* 煙火/彩帶 Canvas */
#confetti-canvas {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    pointer-events: none;
    z-index: 1999;
}

/* 全螢幕模式 */
body.fullscreen .toolbar,
body.fullscreen .control-panel,
body.fullscreen .tabs-container {
    display: none;
}

body.fullscreen .canvas-container {
    height: 100vh;
}

/* 響應式設計 */
@media (max-width: 768px) {
    .winner-name {
        font-size: 40px;
    }

    .winner-prize {
        font-size: 20px;
    }

    .draw-button {
        padding: 12px 35px;
        font-size: 16px;
    }
}
```

- [ ] **Step 4: 建立 UI HTML 結構**

在 `<body>` 標籤內加入：

```html
<!-- 頂部工具列 -->
<div class="toolbar">
    <select id="theme-select">
        <option value="tech">科技感</option>
        <option value="festive">節慶派對</option>
        <option value="minimal">簡約現代</option>
        <option value="custom">自訂</option>
    </select>
    <button id="fullscreen-btn">全螢幕</button>
    <button id="sound-toggle">🔊 音效</button>
</div>

<!-- 3D 動畫區域 -->
<div class="canvas-container">
    <canvas id="canvas3d"></canvas>
    <div class="fallback-2d" id="fallback-2d"></div>
</div>

<!-- 控制區域 -->
<div class="control-panel">
    <div class="prize-display">
        獎項: <span class="prize-name" id="current-prize">一等獎</span>
    </div>
    <button class="draw-button" id="draw-btn">開始抽獎</button>
</div>

<!-- 分頁區域 -->
<div class="tabs-container">
    <div class="tab-buttons">
        <button class="tab-btn active" data-tab="names">名單管理</button>
        <button class="tab-btn" data-tab="prizes">獎項管理</button>
        <button class="tab-btn" data-tab="history">歷史記錄</button>
    </div>

    <!-- 名單管理 -->
    <div class="tab-content active" id="tab-names">
        <div class="list-actions">
            <button id="add-name-btn">新增</button>
            <button id="batch-import-names-btn">批次匯入</button>
            <button id="google-import-btn">Google 匯入</button>
            <button id="clear-names-btn">清除全部</button>
        </div>
        <ul class="item-list" id="names-list"></ul>
    </div>

    <!-- 獎項管理 -->
    <div class="tab-content" id="tab-prizes">
        <div class="list-actions">
            <button id="add-prize-btn">新增</button>
            <button id="batch-import-prizes-btn">批次匯入</button>
            <button id="clear-prizes-btn">清除全部</button>
        </div>
        <ul class="item-list" id="prizes-list"></ul>
    </div>

    <!-- 歷史記錄 -->
    <div class="tab-content" id="tab-history">
        <div class="list-actions">
            <button id="export-csv-btn">匯出 CSV</button>
            <button id="export-json-btn">匯出 JSON</button>
            <button id="clear-history-btn">清除記錄</button>
        </div>
        <ul class="item-list" id="history-list"></ul>
    </div>
</div>

<!-- 批次匯入彈窗 -->
<div class="modal" id="batch-modal">
    <div class="modal-content">
        <div class="modal-header">
            <h3 id="batch-modal-title">批次匯入</h3>
            <button class="modal-close">&times;</button>
        </div>
        <textarea id="batch-input" placeholder="每行一個項目..."></textarea>
        <button class="primary" id="batch-confirm">確認匯入</button>
    </div>
</div>

<!-- Google 匯入彈窗 -->
<div class="modal" id="google-modal">
    <div class="modal-content">
        <div class="modal-header">
            <h3>Google Sheets 匯入</h3>
            <button class="modal-close">&times;</button>
        </div>
        <input type="text" id="google-api-key" placeholder="API Key">
        <input type="text" id="google-sheet-id" placeholder="試算表 ID（網址中 /d/ 後的字串）">
        <input type="text" id="google-range" placeholder="範圍（預設：Sheet1!A2:A）" value="Sheet1!A2:A">
        <button class="primary" id="google-confirm">匯入</button>
    </div>
</div>

<!-- 自訂主題彈窗 -->
<div class="modal" id="theme-modal">
    <div class="modal-content">
        <div class="modal-header">
            <h3>自訂主題</h3>
            <button class="modal-close">&times;</button>
        </div>
        <label>背景顏色</label>
        <input type="color" id="custom-bg" value="#0a0a1a">
        <label>文字顏色</label>
        <input type="color" id="custom-text" value="#ffffff">
        <label>強調色</label>
        <input type="color" id="custom-accent" value="#00ffff">
        <label>特效顏色</label>
        <input type="color" id="custom-particle" value="#00ffff">
        <button class="primary" id="theme-confirm">套用</button>
    </div>
</div>

<!-- 中獎者顯示 -->
<div class="winner-overlay" id="winner-overlay">
    <div class="winner-name" id="winner-name"></div>
    <div class="winner-prize" id="winner-prize"></div>
    <div class="winner-actions">
        <button id="screenshot-btn">📸 截圖</button>
        <button id="close-winner-btn">繼續</button>
    </div>
</div>

<!-- 煙火/彩帶 Canvas -->
<canvas id="confetti-canvas"></canvas>
```

- [ ] **Step 5: 提交基礎結構**

```bash
git add lottery.html
git commit -m "feat: add basic HTML structure and CSS styles"
```

---

## Task 2: 資料管理模組 (localStorage 與狀態)

**Files:**
- Modify: `lottery.html` (script 區域)

- [ ] **Step 1: 建立資料狀態與 localStorage 工具**

在 `<script>` 標籤內加入：

```javascript
// ============ 資料狀態 ============
const state = {
    names: [],           // 名單
    prizes: [],          // 獎項
    history: [],         // 歷史記錄
    currentPrizeIndex: 0, // 當前獎項索引
    isDrawing: false,    // 是否正在抽獎
    soundEnabled: true,  // 音效開關
    theme: 'tech',       // 當前主題
    customTheme: {       // 自訂主題設定
        bgColor: '#0a0a1a',
        textColor: '#ffffff',
        accentColor: '#00ffff',
        particleColor: '#00ffff'
    },
    googleApiKey: '',    // Google API Key
    webglSupported: true // WebGL 支援狀態
};

// ============ localStorage 工具 ============
const Storage = {
    isAvailable() {
        try {
            const test = '__test__';
            localStorage.setItem(test, test);
            localStorage.removeItem(test);
            return true;
        } catch (e) {
            return false;
        }
    },

    save(key, data) {
        if (!this.isAvailable()) {
            console.warn('localStorage 不可用');
            return false;
        }
        try {
            localStorage.setItem(key, JSON.stringify(data));
            return true;
        } catch (e) {
            alert('無法儲存資料，請檢查瀏覽器設定');
            return false;
        }
    },

    load(key, defaultValue = null) {
        if (!this.isAvailable()) return defaultValue;
        try {
            const data = localStorage.getItem(key);
            return data ? JSON.parse(data) : defaultValue;
        } catch (e) {
            return defaultValue;
        }
    },

    remove(key) {
        if (this.isAvailable()) {
            localStorage.removeItem(key);
        }
    }
};

// 儲存鍵值
const STORAGE_KEYS = {
    NAMES: 'lottery_names',
    PRIZES: 'lottery_prizes',
    HISTORY: 'lottery_history',
    THEME: 'lottery_theme',
    CUSTOM_THEME: 'lottery_custom_theme',
    SOUND: 'lottery_sound',
    GOOGLE_API_KEY: 'lottery_google_api_key'
};
```

- [ ] **Step 2: 建立資料載入與儲存函數**

```javascript
// ============ 資料載入與儲存 ============
function loadState() {
    state.names = Storage.load(STORAGE_KEYS.NAMES, []);
    state.prizes = Storage.load(STORAGE_KEYS.PRIZES, []);
    state.history = Storage.load(STORAGE_KEYS.HISTORY, []);
    state.theme = Storage.load(STORAGE_KEYS.THEME, 'tech');
    state.customTheme = Storage.load(STORAGE_KEYS.CUSTOM_THEME, state.customTheme);
    state.soundEnabled = Storage.load(STORAGE_KEYS.SOUND, true);
    state.googleApiKey = Storage.load(STORAGE_KEYS.GOOGLE_API_KEY, '');
}

function saveNames() {
    Storage.save(STORAGE_KEYS.NAMES, state.names);
    renderNamesList();
    update3DScene();
}

function savePrizes() {
    Storage.save(STORAGE_KEYS.PRIZES, state.prizes);
    renderPrizesList();
    updateCurrentPrizeDisplay();
}

function saveHistory() {
    Storage.save(STORAGE_KEYS.HISTORY, state.history);
    renderHistoryList();
}

function saveTheme() {
    Storage.save(STORAGE_KEYS.THEME, state.theme);
    Storage.save(STORAGE_KEYS.CUSTOM_THEME, state.customTheme);
}

function saveSoundSetting() {
    Storage.save(STORAGE_KEYS.SOUND, state.soundEnabled);
}
```

- [ ] **Step 3: 建立 UUID 產生器**

```javascript
// ============ 工具函數 ============
function generateUUID() {
    return 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, function(c) {
        const r = Math.random() * 16 | 0;
        const v = c === 'x' ? r : (r & 0x3 | 0x8);
        return v.toString(16);
    });
}

function formatTimestamp(timestamp) {
    const date = new Date(timestamp);
    return date.toLocaleString('zh-TW');
}
```

- [ ] **Step 4: 提交資料管理模組**

```bash
git add lottery.html
git commit -m "feat: add data management module with localStorage"
```

---

## Task 3: UI 互動功能 (列表管理、分頁、彈窗)

**Files:**
- Modify: `lottery.html`

- [ ] **Step 1: 分頁切換功能**

```javascript
// ============ 分頁切換 ============
function initTabs() {
    const tabBtns = document.querySelectorAll('.tab-btn');
    tabBtns.forEach(btn => {
        btn.addEventListener('click', () => {
            // 移除所有 active
            document.querySelectorAll('.tab-btn').forEach(b => b.classList.remove('active'));
            document.querySelectorAll('.tab-content').forEach(c => c.classList.remove('active'));

            // 加入 active
            btn.classList.add('active');
            const tabId = 'tab-' + btn.dataset.tab;
            document.getElementById(tabId).classList.add('active');
        });
    });
}
```

- [ ] **Step 2: 名單列表渲染與操作**

```javascript
// ============ 名單管理 ============
function renderNamesList() {
    const list = document.getElementById('names-list');
    list.innerHTML = '';

    state.names.forEach((name, index) => {
        const li = document.createElement('li');
        li.draggable = true;
        li.dataset.index = index;
        li.innerHTML = `
            <span class="item-name">${name}</span>
            <div class="item-actions">
                <button onclick="editName(${index})">編輯</button>
                <button onclick="deleteName(${index})">刪除</button>
            </div>
        `;

        // 拖曳事件
        li.addEventListener('dragstart', handleDragStart);
        li.addEventListener('dragover', handleDragOver);
        li.addEventListener('drop', handleDrop);
        li.addEventListener('dragend', handleDragEnd);

        list.appendChild(li);
    });
}

function addName(name) {
    if (name.trim()) {
        state.names.push(name.trim());
        saveNames();
    }
}

function editName(index) {
    const newName = prompt('編輯名字:', state.names[index]);
    if (newName !== null && newName.trim()) {
        state.names[index] = newName.trim();
        saveNames();
    }
}

function deleteName(index) {
    if (confirm('確定要刪除嗎？')) {
        state.names.splice(index, 1);
        saveNames();
    }
}

function clearNames() {
    if (confirm('確定要清除所有名單嗎？')) {
        state.names = [];
        saveNames();
    }
}

// 拖曳排序
let draggedItem = null;

function handleDragStart(e) {
    draggedItem = this;
    this.style.opacity = '0.5';
}

function handleDragOver(e) {
    e.preventDefault();
}

function handleDrop(e) {
    e.preventDefault();
    if (draggedItem !== this) {
        const fromIndex = parseInt(draggedItem.dataset.index);
        const toIndex = parseInt(this.dataset.index);

        const item = state.names.splice(fromIndex, 1)[0];
        state.names.splice(toIndex, 0, item);
        saveNames();
    }
}

function handleDragEnd() {
    this.style.opacity = '1';
}
```

- [ ] **Step 3: 獎項列表渲染與操作**

```javascript
// ============ 獎項管理 ============
function renderPrizesList() {
    const list = document.getElementById('prizes-list');
    list.innerHTML = '';

    state.prizes.forEach((prize, index) => {
        const li = document.createElement('li');
        li.draggable = true;
        li.dataset.index = index;
        li.innerHTML = `
            <span class="item-name">${prize}</span>
            <div class="item-actions">
                <button onclick="editPrize(${index})">編輯</button>
                <button onclick="deletePrize(${index})">刪除</button>
            </div>
        `;

        li.addEventListener('dragstart', handleDragStart);
        li.addEventListener('dragover', handleDragOver);
        li.addEventListener('drop', handleDrop);
        li.addEventListener('dragend', handleDragEnd);

        list.appendChild(li);
    });
}

function addPrize(prize) {
    if (prize.trim()) {
        state.prizes.push(prize.trim());
        savePrizes();
    }
}

function editPrize(index) {
    const newPrize = prompt('編輯獎項:', state.prizes[index]);
    if (newPrize !== null && newPrize.trim()) {
        state.prizes[index] = newPrize.trim();
        savePrizes();
    }
}

function deletePrize(index) {
    if (confirm('確定要刪除嗎？')) {
        state.prizes.splice(index, 1);
        savePrizes();
    }
}

function clearPrizes() {
    if (confirm('確定要清除所有獎項嗎？')) {
        state.prizes = [];
        savePrizes();
    }
}

function updateCurrentPrizeDisplay() {
    const display = document.getElementById('current-prize');
    if (state.prizes.length > 0) {
        display.textContent = state.prizes[0];
    } else {
        display.textContent = '無（繼續抽名）';
    }
}
```

- [ ] **Step 4: 歷史記錄渲染**

```javascript
// ============ 歷史記錄 ============
function renderHistoryList() {
    const list = document.getElementById('history-list');
    list.innerHTML = '';

    state.history.slice().reverse().forEach((record, index) => {
        const li = document.createElement('li');
        li.innerHTML = `
            <span class="item-name">
                ${record.prize ? `[${record.prize}] ` : ''}${record.winner}
                <small style="opacity:0.6; margin-left:10px">${formatTimestamp(record.timestamp)}</small>
            </span>
        `;
        list.appendChild(li);
    });
}

function addHistoryRecord(winner, prize) {
    state.history.push({
        id: generateUUID(),
        timestamp: Date.now(),
        prize: prize,
        winner: winner
    });
    saveHistory();
}

function clearHistory() {
    if (confirm('確定要清除所有歷史記錄嗎？')) {
        state.history = [];
        saveHistory();
    }
}
```

- [ ] **Step 5: 彈窗控制**

```javascript
// ============ 彈窗控制 ============
function showModal(modalId) {
    document.getElementById(modalId).classList.add('show');
}

function hideModal(modalId) {
    document.getElementById(modalId).classList.remove('show');
}

function initModals() {
    // 關閉按鈕
    document.querySelectorAll('.modal-close').forEach(btn => {
        btn.addEventListener('click', () => {
            btn.closest('.modal').classList.remove('show');
        });
    });

    // 點擊背景關閉
    document.querySelectorAll('.modal').forEach(modal => {
        modal.addEventListener('click', (e) => {
            if (e.target === modal) {
                modal.classList.remove('show');
            }
        });
    });
}
```

- [ ] **Step 6: 批次匯入功能**

```javascript
// ============ 批次匯入 ============
let batchImportTarget = null;

function openBatchImport(target) {
    batchImportTarget = target;
    const title = target === 'names' ? '批次匯入名單' : '批次匯入獎項';
    document.getElementById('batch-modal-title').textContent = title;
    document.getElementById('batch-input').value = '';
    showModal('batch-modal');
}

function confirmBatchImport() {
    const input = document.getElementById('batch-input').value;
    const items = input.split('\n').map(s => s.trim()).filter(s => s);

    if (batchImportTarget === 'names') {
        state.names = [...state.names, ...items];
        saveNames();
    } else {
        state.prizes = [...state.prizes, ...items];
        savePrizes();
    }

    hideModal('batch-modal');
}
```

- [ ] **Step 7: 綁定按鈕事件**

```javascript
// ============ 綁定按鈕事件 ============
function initButtons() {
    // 名單管理
    document.getElementById('add-name-btn').addEventListener('click', () => {
        const name = prompt('輸入名字:');
        if (name) addName(name);
    });
    document.getElementById('batch-import-names-btn').addEventListener('click', () => openBatchImport('names'));
    document.getElementById('clear-names-btn').addEventListener('click', clearNames);

    // 獎項管理
    document.getElementById('add-prize-btn').addEventListener('click', () => {
        const prize = prompt('輸入獎項:');
        if (prize) addPrize(prize);
    });
    document.getElementById('batch-import-prizes-btn').addEventListener('click', () => openBatchImport('prizes'));
    document.getElementById('clear-prizes-btn').addEventListener('click', clearPrizes);

    // 歷史記錄
    document.getElementById('clear-history-btn').addEventListener('click', clearHistory);

    // 批次匯入確認
    document.getElementById('batch-confirm').addEventListener('click', confirmBatchImport);
}
```

- [ ] **Step 8: 提交 UI 互動功能**

```bash
git add lottery.html
git commit -m "feat: add UI interaction (tabs, lists, modals, batch import)"
```

---

## Task 4: Google Sheets 匯入功能

**Files:**
- Modify: `lottery.html`

- [ ] **Step 1: Google Sheets API 匯入函數**

```javascript
// ============ Google Sheets 匯入 ============
async function importFromGoogleSheets() {
    const apiKey = document.getElementById('google-api-key').value.trim();
    const sheetId = document.getElementById('google-sheet-id').value.trim();
    const range = document.getElementById('google-range').value.trim() || 'Sheet1!A2:A';

    if (!apiKey || !sheetId) {
        alert('請輸入 API Key 和試算表 ID');
        return;
    }

    // 儲存 API Key
    state.googleApiKey = apiKey;
    Storage.save(STORAGE_KEYS.GOOGLE_API_KEY, apiKey);

    const url = `https://sheets.googleapis.com/v4/spreadsheets/${sheetId}/values/${range}?key=${apiKey}`;

    try {
        const response = await fetch(url);

        if (!response.ok) {
            if (response.status === 400 || response.status === 403) {
                throw new Error('API Key 無效，請檢查設定');
            } else if (response.status === 404) {
                throw new Error('無法讀取試算表，請確認已發布為網頁');
            } else {
                throw new Error('匯入失敗，請稍後再試');
            }
        }

        const data = await response.json();

        if (!data.values || data.values.length === 0) {
            throw new Error('試算表沒有資料');
        }

        // 將二維陣列轉為一維
        const items = data.values.flat().filter(s => s && s.trim());

        state.names = [...state.names, ...items];
        saveNames();

        hideModal('google-modal');
        alert(`成功匯入 ${items.length} 筆資料`);

    } catch (error) {
        alert(error.message);
    }
}
```

- [ ] **Step 2: 綁定 Google 匯入按鈕**

```javascript
// 在 initButtons() 函數中加入：

// Google 匯入
document.getElementById('google-import-btn').addEventListener('click', () => {
    document.getElementById('google-api-key').value = state.googleApiKey;
    showModal('google-modal');
});

document.getElementById('google-confirm').addEventListener('click', importFromGoogleSheets);
```

- [ ] **Step 3: 提交 Google Sheets 匯入功能**

```bash
git add lottery.html
git commit -m "feat: add Google Sheets import functionality"
```

---

## Task 5: 主題系統

**Files:**
- Modify: `lottery.html`

- [ ] **Step 1: 主題定義與切換函數**

```javascript
// ============ 主題系統 ============
const themes = {
    tech: {
        bgColor: '#0a0a1a',
        textColor: '#ffffff',
        accentColor: '#00ffff',
        particleColor: '#00ffff'
    },
    festive: {
        bgColor: '#1a0a1a',
        textColor: '#ffffff',
        accentColor: '#ffd700',
        particleColor: '#ff69b4'
    },
    minimal: {
        bgColor: '#f5f5f5',
        textColor: '#333333',
        accentColor: '#0066cc',
        particleColor: '#0066cc'
    }
};

function applyTheme(themeName) {
    let theme;

    if (themeName === 'custom') {
        theme = state.customTheme;
        showModal('theme-modal');
    } else {
        theme = themes[themeName];
    }

    document.documentElement.style.setProperty('--bg-color', theme.bgColor);
    document.documentElement.style.setProperty('--text-color', theme.textColor);
    document.documentElement.style.setProperty('--accent-color', theme.accentColor);
    document.documentElement.style.setProperty('--particle-color', theme.particleColor);

    state.theme = themeName;
    saveTheme();
}

function applyCustomTheme() {
    state.customTheme = {
        bgColor: document.getElementById('custom-bg').value,
        textColor: document.getElementById('custom-text').value,
        accentColor: document.getElementById('custom-accent').value,
        particleColor: document.getElementById('custom-particle').value
    };

    applyTheme('custom');
    hideModal('theme-modal');
}
```

- [ ] **Step 2: 綁定主題切換事件**

```javascript
// 在 initButtons() 或初始化函數中加入：

document.getElementById('theme-select').addEventListener('change', (e) => {
    applyTheme(e.target.value);
});

document.getElementById('theme-confirm').addEventListener('click', applyCustomTheme);
```

- [ ] **Step 3: 提交主題系統**

```bash
git add lottery.html
git commit -m "feat: add theme system with 3 presets and custom option"
```

---

## Task 6: 音效系統

**Files:**
- Modify: `lottery.html`

- [ ] **Step 1: 加入音效 (base64) 與播放函數**

```javascript
// ============ 音效系統 ============
// 使用簡單的合成音效（實際專案可替換為真實 base64 音效）
const AudioSystem = {
    audioContext: null,

    init() {
        try {
            this.audioContext = new (window.AudioContext || window.webkitAudioContext)();
        } catch (e) {
            console.warn('Web Audio API 不支援');
        }
    },

    // 滾動音效 - 快速滴答聲
    playTick() {
        if (!state.soundEnabled || !this.audioContext) return;

        const osc = this.audioContext.createOscillator();
        const gain = this.audioContext.createGain();

        osc.connect(gain);
        gain.connect(this.audioContext.destination);

        osc.frequency.value = 800 + Math.random() * 400;
        osc.type = 'sine';

        gain.gain.setValueAtTime(0.1, this.audioContext.currentTime);
        gain.gain.exponentialRampToValueAtTime(0.01, this.audioContext.currentTime + 0.05);

        osc.start(this.audioContext.currentTime);
        osc.stop(this.audioContext.currentTime + 0.05);
    },

    // 中獎音效 - 上升音階
    playWin() {
        if (!state.soundEnabled || !this.audioContext) return;

        const notes = [523.25, 659.25, 783.99, 1046.50]; // C5, E5, G5, C6

        notes.forEach((freq, i) => {
            setTimeout(() => {
                const osc = this.audioContext.createOscillator();
                const gain = this.audioContext.createGain();

                osc.connect(gain);
                gain.connect(this.audioContext.destination);

                osc.frequency.value = freq;
                osc.type = 'sine';

                gain.gain.setValueAtTime(0.2, this.audioContext.currentTime);
                gain.gain.exponentialRampToValueAtTime(0.01, this.audioContext.currentTime + 0.3);

                osc.start(this.audioContext.currentTime);
                osc.stop(this.audioContext.currentTime + 0.3);
            }, i * 100);
        });
    },

    // 切換音效
    toggle() {
        state.soundEnabled = !state.soundEnabled;
        const btn = document.getElementById('sound-toggle');
        btn.textContent = state.soundEnabled ? '🔊 音效' : '🔇 靜音';
        saveSoundSetting();
    }
};
```

- [ ] **Step 2: 綁定音效切換按鈕**

```javascript
// 在初始化函數中加入：

document.getElementById('sound-toggle').addEventListener('click', () => AudioSystem.toggle());
```

- [ ] **Step 3: 提交音效系統**

```bash
git add lottery.html
git commit -m "feat: add sound system with Web Audio API"
```

---

## Task 7: 3D 球體動畫 (Three.js)

**Files:**
- Modify: `lottery.html`

- [ ] **Step 1: WebGL 支援檢測**

```javascript
// ============ WebGL 檢測 ============
function checkWebGL() {
    try {
        const canvas = document.createElement('canvas');
        return !!(window.WebGLRenderingContext &&
            (canvas.getContext('webgl') || canvas.getContext('experimental-webgl')));
    } catch (e) {
        return false;
    }
}
```

- [ ] **Step 2: Three.js 3D 場景初始化**

```javascript
// ============ Three.js 3D 場景 ============
let scene, camera, renderer, sphereGroup, nameSprites = [];
let animationId = null;

function init3DScene() {
    state.webglSupported = checkWebGL();

    if (!state.webglSupported) {
        document.getElementById('canvas3d').style.display = 'none';
        document.getElementById('fallback-2d').style.display = 'block';
        init2DFallback();
        return;
    }

    const container = document.getElementById('canvas3d');
    const width = container.parentElement.clientWidth;
    const height = container.parentElement.clientHeight;

    // 場景
    scene = new THREE.Scene();

    // 相機
    camera = new THREE.PerspectiveCamera(75, width / height, 0.1, 1000);
    camera.position.z = 300;

    // 渲染器
    renderer = new THREE.WebGLRenderer({ canvas: container, antialias: true, alpha: true });
    renderer.setSize(width, height);
    renderer.setPixelRatio(window.devicePixelRatio);

    // 球體群組
    sphereGroup = new THREE.Group();
    scene.add(sphereGroup);

    // 燈光
    const ambientLight = new THREE.AmbientLight(0xffffff, 0.5);
    scene.add(ambientLight);

    const pointLight = new THREE.PointLight(0xffffff, 1);
    pointLight.position.set(100, 100, 100);
    scene.add(pointLight);

    // 初始渲染
    update3DScene();
    animate();

    // 視窗大小調整
    window.addEventListener('resize', onWindowResize);
}

function onWindowResize() {
    const container = document.getElementById('canvas3d').parentElement;
    const width = container.clientWidth;
    const height = container.clientHeight;

    camera.aspect = width / height;
    camera.updateProjectionMatrix();
    renderer.setSize(width, height);
}
```

- [ ] **Step 3: 更新 3D 場景中的名字**

```javascript
function update3DScene() {
    if (!state.webglSupported) {
        update2DFallback();
        return;
    }

    // 清除舊的名字
    nameSprites.forEach(sprite => sphereGroup.remove(sprite));
    nameSprites = [];

    // 取得要顯示的名字（最多 50 個）
    const displayNames = state.names.length > 50
        ? getRandomItems(state.names, 50)
        : [...state.names];

    // 建立名字精靈
    displayNames.forEach((name, index) => {
        const sprite = createTextSprite(name);
        const phi = Math.acos(-1 + (2 * index) / displayNames.length);
        const theta = Math.sqrt(displayNames.length * Math.PI) * phi;
        const radius = 100;

        sprite.position.x = radius * Math.cos(theta) * Math.sin(phi);
        sprite.position.y = radius * Math.sin(theta) * Math.sin(phi);
        sprite.position.z = radius * Math.cos(phi);

        sphereGroup.add(sprite);
        nameSprites.push(sprite);
    });
}

function createTextSprite(text) {
    const canvas = document.createElement('canvas');
    const context = canvas.getContext('2d');
    canvas.width = 256;
    canvas.height = 64;

    context.fillStyle = 'transparent';
    context.fillRect(0, 0, canvas.width, canvas.height);

    context.font = 'bold 24px Microsoft JhengHei, sans-serif';
    context.fillStyle = getComputedStyle(document.documentElement).getPropertyValue('--text-color');
    context.textAlign = 'center';
    context.textBaseline = 'middle';
    context.fillText(text, canvas.width / 2, canvas.height / 2);

    const texture = new THREE.CanvasTexture(canvas);
    const material = new THREE.SpriteMaterial({ map: texture, transparent: true });
    const sprite = new THREE.Sprite(material);
    sprite.scale.set(50, 12.5, 1);

    return sprite;
}

function getRandomItems(arr, count) {
    const shuffled = [...arr].sort(() => Math.random() - 0.5);
    return shuffled.slice(0, count);
}
```

- [ ] **Step 4: 動畫循環**

```javascript
let rotationSpeed = 0.005;
let targetSpeed = 0.005;

function animate() {
    animationId = requestAnimationFrame(animate);

    // 平滑過渡速度
    rotationSpeed += (targetSpeed - rotationSpeed) * 0.02;

    if (sphereGroup) {
        sphereGroup.rotation.y += rotationSpeed;
        sphereGroup.rotation.x += rotationSpeed * 0.3;
    }

    if (renderer && scene && camera) {
        renderer.render(scene, camera);
    }
}
```

- [ ] **Step 5: 2D 備用模式**

```javascript
// ============ 2D 備用模式 ============
function init2DFallback() {
    const container = document.getElementById('fallback-2d');
    container.innerHTML = '<div class="fallback-content"></div>';
}

function update2DFallback() {
    const container = document.getElementById('fallback-2d');
    const displayNames = state.names.length > 50
        ? getRandomItems(state.names, 50)
        : [...state.names];

    container.innerHTML = displayNames
        .map(name => `<span class="name-item">${name}</span>`)
        .join('');
}
```

- [ ] **Step 6: 提交 3D 場景**

```bash
git add lottery.html
git commit -m "feat: add 3D sphere scene with Three.js and 2D fallback"
```

---

## Task 8: 抽獎邏輯與動畫

**Files:**
- Modify: `lottery.html`

- [ ] **Step 1: 抽獎核心邏輯**

```javascript
// ============ 抽獎邏輯 ============
function startDraw() {
    if (state.isDrawing) return;

    if (state.names.length === 0) {
        alert('請先加入名單');
        return;
    }

    state.isDrawing = true;
    document.getElementById('draw-btn').disabled = true;

    // 重新隨機選擇顯示的名字
    update3DScene();

    // 開始旋轉動畫
    startSpinAnimation();
}

function startSpinAnimation() {
    // 加速旋轉
    targetSpeed = 0.15;

    // 播放滾動音效
    const tickInterval = setInterval(() => {
        AudioSystem.playTick();
    }, 100);

    // 3 秒後開始減速
    setTimeout(() => {
        clearInterval(tickInterval);
        slowDownAnimation();
    }, 3000);
}

function slowDownAnimation() {
    // 漸漸減速
    targetSpeed = 0.005;

    // 2 秒後揭曉
    setTimeout(() => {
        revealWinner();
    }, 2000);
}

function revealWinner() {
    // 隨機選擇中獎者
    const winnerIndex = Math.floor(Math.random() * state.names.length);
    const winner = state.names[winnerIndex];

    // 取得獎項
    let prize = null;
    if (state.prizes.length > 0) {
        prize = state.prizes.shift();
        savePrizes();
    }

    // 從名單移除中獎者
    state.names.splice(winnerIndex, 1);
    saveNames();

    // 記錄歷史
    addHistoryRecord(winner, prize);

    // 播放中獎音效
    AudioSystem.playWin();

    // 顯示中獎者
    showWinnerOverlay(winner, prize);

    state.isDrawing = false;
    document.getElementById('draw-btn').disabled = false;
}
```

- [ ] **Step 2: 中獎者顯示 Overlay**

```javascript
function showWinnerOverlay(winner, prize) {
    document.getElementById('winner-name').textContent = winner;
    document.getElementById('winner-prize').textContent = prize ? `🎉 ${prize}` : '';

    document.getElementById('winner-overlay').classList.add('show');

    // 啟動煙火/彩帶動畫
    startConfetti();
}

function hideWinnerOverlay() {
    document.getElementById('winner-overlay').classList.remove('show');
    stopConfetti();

    // 更新顯示
    updateCurrentPrizeDisplay();

    // 如果名單空了，提示
    if (state.names.length === 0) {
        alert('名單已抽完！');
    }
}
```

- [ ] **Step 3: 綁定抽獎按鈕**

```javascript
// 在初始化函數中加入：

document.getElementById('draw-btn').addEventListener('click', startDraw);
document.getElementById('close-winner-btn').addEventListener('click', hideWinnerOverlay);
```

- [ ] **Step 4: 提交抽獎邏輯**

```bash
git add lottery.html
git commit -m "feat: add lottery draw logic with spin animation"
```

---

## Task 9: 煙火/彩帶動畫

**Files:**
- Modify: `lottery.html`

- [ ] **Step 1: 煙火/彩帶粒子系統**

```javascript
// ============ 煙火/彩帶動畫 ============
let confettiParticles = [];
let confettiAnimationId = null;
const confettiCanvas = document.getElementById('confetti-canvas');
const confettiCtx = confettiCanvas.getContext('2d');

function initConfettiCanvas() {
    confettiCanvas.width = window.innerWidth;
    confettiCanvas.height = window.innerHeight;
    window.addEventListener('resize', () => {
        confettiCanvas.width = window.innerWidth;
        confettiCanvas.height = window.innerHeight;
    });
}

function createConfettiParticle() {
    const colors = [
        getComputedStyle(document.documentElement).getPropertyValue('--accent-color').trim(),
        '#ff0000', '#00ff00', '#0000ff', '#ffff00', '#ff00ff', '#00ffff'
    ];

    return {
        x: Math.random() * confettiCanvas.width,
        y: -20,
        size: Math.random() * 10 + 5,
        color: colors[Math.floor(Math.random() * colors.length)],
        speedY: Math.random() * 3 + 2,
        speedX: Math.random() * 4 - 2,
        rotation: Math.random() * 360,
        rotationSpeed: Math.random() * 10 - 5
    };
}

function startConfetti() {
    confettiParticles = [];

    // 創建大量粒子
    for (let i = 0; i < 150; i++) {
        setTimeout(() => {
            confettiParticles.push(createConfettiParticle());
        }, i * 20);
    }

    animateConfetti();
}

function animateConfetti() {
    confettiCtx.clearRect(0, 0, confettiCanvas.width, confettiCanvas.height);

    confettiParticles.forEach((p, index) => {
        p.y += p.speedY;
        p.x += p.speedX;
        p.rotation += p.rotationSpeed;

        // 畫彩帶
        confettiCtx.save();
        confettiCtx.translate(p.x, p.y);
        confettiCtx.rotate(p.rotation * Math.PI / 180);
        confettiCtx.fillStyle = p.color;
        confettiCtx.fillRect(-p.size / 2, -p.size / 4, p.size, p.size / 2);
        confettiCtx.restore();

        // 移除超出畫面的粒子
        if (p.y > confettiCanvas.height) {
            confettiParticles.splice(index, 1);
        }
    });

    if (confettiParticles.length > 0) {
        confettiAnimationId = requestAnimationFrame(animateConfetti);
    }
}

function stopConfetti() {
    if (confettiAnimationId) {
        cancelAnimationFrame(confettiAnimationId);
    }
    confettiCtx.clearRect(0, 0, confettiCanvas.width, confettiCanvas.height);
    confettiParticles = [];
}
```

- [ ] **Step 2: 提交煙火動畫**

```bash
git add lottery.html
git commit -m "feat: add confetti particle animation for winner celebration"
```

---

## Task 10: 全螢幕模式

**Files:**
- Modify: `lottery.html`

- [ ] **Step 1: 全螢幕切換功能**

```javascript
// ============ 全螢幕模式 ============
function toggleFullscreen() {
    if (!document.fullscreenElement) {
        document.documentElement.requestFullscreen().catch(err => {
            alert('無法進入全螢幕模式');
        });
        document.body.classList.add('fullscreen');
    } else {
        document.exitFullscreen();
        document.body.classList.remove('fullscreen');
    }
}

document.addEventListener('fullscreenchange', () => {
    if (!document.fullscreenElement) {
        document.body.classList.remove('fullscreen');
    }
});

// 快速鍵 F
document.addEventListener('keydown', (e) => {
    if (e.key === 'f' || e.key === 'F') {
        toggleFullscreen();
    }
});
```

- [ ] **Step 2: 綁定全螢幕按鈕**

```javascript
// 在初始化函數中加入：

document.getElementById('fullscreen-btn').addEventListener('click', toggleFullscreen);
```

- [ ] **Step 3: 提交全螢幕功能**

```bash
git add lottery.html
git commit -m "feat: add fullscreen mode with F hotkey"
```

---

## Task 11: 匯出功能

**Files:**
- Modify: `lottery.html`

- [ ] **Step 1: CSV 匯出**

```javascript
// ============ 匯出功能 ============
function exportCSV() {
    if (state.history.length === 0) {
        alert('沒有歷史記錄可匯出');
        return;
    }

    let csv = '\uFEFF'; // BOM for Excel
    csv += '抽獎序號,時間,獎項,中獎者\n';

    state.history.forEach((record, index) => {
        const time = formatTimestamp(record.timestamp);
        const prize = record.prize || '';
        csv += `${index + 1},${time},${prize},${record.winner}\n`;
    });

    downloadFile(csv, 'lottery-result.csv', 'text/csv;charset=utf-8');
}
```

- [ ] **Step 2: JSON 匯出**

```javascript
function exportJSON() {
    if (state.history.length === 0) {
        alert('沒有歷史記錄可匯出');
        return;
    }

    const json = JSON.stringify(state.history, null, 2);
    downloadFile(json, 'lottery-result.json', 'application/json');
}

function downloadFile(content, filename, type) {
    const blob = new Blob([content], { type });
    const url = URL.createObjectURL(blob);
    const a = document.createElement('a');
    a.href = url;
    a.download = filename;
    a.click();
    URL.revokeObjectURL(url);
}
```

- [ ] **Step 3: 截圖功能**

```javascript
function takeScreenshot() {
    const overlay = document.getElementById('winner-overlay');

    html2canvas(overlay).then(canvas => {
        const link = document.createElement('a');
        link.download = `winner-${Date.now()}.png`;
        link.href = canvas.toDataURL();
        link.click();
    });
}
```

- [ ] **Step 4: 綁定匯出按鈕**

```javascript
// 在初始化函數中加入：

document.getElementById('export-csv-btn').addEventListener('click', exportCSV);
document.getElementById('export-json-btn').addEventListener('click', exportJSON);
document.getElementById('screenshot-btn').addEventListener('click', takeScreenshot);
```

- [ ] **Step 5: 提交匯出功能**

```bash
git add lottery.html
git commit -m "feat: add export functionality (CSV, JSON, screenshot)"
```

---

## Task 12: 應用程式初始化

**Files:**
- Modify: `lottery.html`

- [ ] **Step 1: 主初始化函數**

```javascript
// ============ 初始化 ============
function init() {
    // 載入儲存的資料
    loadState();

    // 初始化各模組
    initTabs();
    initModals();
    initButtons();
    initConfettiCanvas();
    AudioSystem.init();

    // 初始化 3D 場景
    init3DScene();

    // 渲染列表
    renderNamesList();
    renderPrizesList();
    renderHistoryList();

    // 更新顯示
    updateCurrentPrizeDisplay();

    // 套用主題
    document.getElementById('theme-select').value = state.theme;
    applyTheme(state.theme);

    // 更新音效按鈕
    document.getElementById('sound-toggle').textContent = state.soundEnabled ? '🔊 音效' : '🔇 靜音';

    console.log('3D 抽籤系統已初始化');
}

// 頁面載入完成後初始化
window.addEventListener('DOMContentLoaded', init);
```

- [ ] **Step 2: 最終提交**

```bash
git add lottery.html
git commit -m "feat: complete 3D lottery system initialization"
```

---

## Task 13: 測試與驗證

- [ ] **Step 1: 在瀏覽器中開啟測試**

```bash
# macOS
open lottery.html

# 或使用 Python 簡易伺服器
python3 -m http.server 8080
# 然後開啟 http://localhost:8080/lottery.html
```

- [ ] **Step 2: 功能檢查清單**

測試項目：
- [ ] 頁面正常載入，3D 球體顯示
- [ ] 新增/編輯/刪除名單
- [ ] 新增/編輯/刪除獎項
- [ ] 批次匯入名單/獎項
- [ ] 拖曳排序
- [ ] 主題切換（科技感、節慶派對、簡約現代、自訂）
- [ ] 音效開關
- [ ] 全螢幕模式（F 鍵）
- [ ] 抽獎動畫（旋轉加速→減速→揭曉）
- [ ] 中獎者顯示與煙火動畫
- [ ] 歷史記錄
- [ ] 匯出 CSV/JSON
- [ ] 截圖功能
- [ ] 頁面重新載入後資料保留

- [ ] **Step 3: 最終提交**

```bash
git add -A
git commit -m "test: verify all features working correctly"
```

---

## 完成標準

1. 所有功能正常運作
2. 3D 動畫流暢
3. 資料正確儲存與載入
4. 匯出功能正常
5. 響應式設計在手機/平板/桌面都正常顯示
